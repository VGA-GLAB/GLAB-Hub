{
  "name": "glab-bot",
  "version": "0.1.0",
  "private": true,
  "description": "GLAB Discord 通知 Bot — 日次・イベント・就活等の通知。GLAB hub と同じ corpus.db を共有",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "tsx --env-file-if-exists=.env index.ts",
    "dev": "tsx watch --env-file-if-exists=.env index.ts",
    "config-setup": "tsx config-setup.ts",
    "register": "tsx register.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@ludiars/encrypted-config": "0.1.0",
    "better-sqlite3": "13.0.3",
    "discord.js": "^14.26.5",
    "tsx": "^4.20.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "7.6.13",
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  },
  "overrides": {
    "undici": "6.27.0"
  }
}
