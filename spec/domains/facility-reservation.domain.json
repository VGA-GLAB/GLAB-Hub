{
  "name": "facility-reservation",
  "description": "Aedilis の施設・予約 API の中継。project token を付けて /api/facilities・/api/reservations を呼ぶだけのコネクタで、施設データを GLAB に複製しない。接続先未設定時は停止せず「未接続」を表示する degraded モードで動く。",
  "presetRules": [],
  "templateRules": [],
  "membership": [
    {
      "pathPattern": "/plugins/facility/"
    },
    {
      "pathPattern": "(^|/)plugins/facility/(?:.*/)?[^/]+$"
    },
    {
      "pathPattern": "(^|/)tests/facilit[a-z0-9-]*\\.test\\.ts$"
    }
  ],
  "cardTemplate": "Summarise the \"facility-reservation\" domain: Aedilis の施設・予約 API の中継。project token を付けて /api/facilities・/api/reservations を呼ぶだけのコネクタで、施設データを GLAB に複製しない。接続先未設定時は停止せず「未接続」を表示する degraded モードで動く。",
  "mechanics": [
    "connector-relay",
    "degraded-mode"
  ],
  "specRefs": [
    "spec/feature/facility.md",
    "spec/interface/aedilis-connector.md"
  ],
  "rationale": "GLAB DESIGN.md 2〜7 章と plugins/pack.json のモジュール構成から、責務境界ごとに手で定義した。"
}
